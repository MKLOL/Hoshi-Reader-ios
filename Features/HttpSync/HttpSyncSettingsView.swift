//
//  HttpSyncSettingsView.swift
//  Hoshi Reader
//
//  Copyright © 2026 Manhhao.
//  SPDX-License-Identifier: GPL-3.0-or-later
//
//  PHASE 0 CONTRACT STUB — replaced by Agent D with the real HTTP KV sync settings screen (base
//  URL + bearer token in Keychain, manual/auto sync). Keep `HttpSyncSettingsView()` so the
//  Settings registration call site stays stable.
//

import SwiftUI

struct HttpSyncSettingsView: View {
    var body: some View {
        Text("HTTP sync settings placeholder")
            .padding()
    }
}
